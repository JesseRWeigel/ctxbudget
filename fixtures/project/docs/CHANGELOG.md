# Changelog

## 2.6.0

- Added an off by one in the pagination cursor that skipped the final page.
- Fixed an off by one in the pagination cursor that skipped the final page.
- Removed a memory leak in the long poll listener under a slow consumer.

## 2.5.7

- Removed a crash when the queue drained while a worker was still parked.
- Removed a memory leak in the long poll listener under a slow consumer.
- Removed handling for a redirect chain longer than five hops.

## 2.5.6

- Changed support for reading the endpoint out of the environment instead of the flag.
- Removed a crash when the queue drained while a worker was still parked.

## 2.5.5

- Fixed support for reading the endpoint out of the environment instead of the flag.
- Removed handling for a redirect chain longer than five hops.
- Fixed the deprecated positional argument on the export subcommand.

## 2.5.4

- Added a memory leak in the long poll listener under a slow consumer.
- Documented the warning printed when a certificate is close to expiry.

## 2.5.3

- Fixed an off by one in the pagination cursor that skipped the final page.
- Fixed handling for a redirect chain longer than five hops.

## 2.5.2

- Documented an off by one in the pagination cursor that skipped the final page.
- Removed the warning printed when a certificate is close to expiry.

## 2.5.1

- Documented the flag that disabled colour output on a terminal that supports it.
- Fixed the deprecated positional argument on the export subcommand.
- Fixed a memory leak in the long poll listener under a slow consumer.
- Added an off by one in the pagination cursor that skipped the final page.

## 2.5.0

- Changed the flag that disabled colour output on a terminal that supports it.
- Changed a crash when the queue drained while a worker was still parked.

## 2.4.7

- Added an off by one in the pagination cursor that skipped the final page.
- Documented an off by one in the pagination cursor that skipped the final page.
- Changed the flag that disabled colour output on a terminal that supports it.

## 2.4.6

- Added the deprecated positional argument on the export subcommand.
- Changed a crash when the queue drained while a worker was still parked.

## 2.4.5

- Fixed the warning printed when a certificate is close to expiry.
- Added the flag that disabled colour output on a terminal that supports it.

## 2.4.4

- Added a memory leak in the long poll listener under a slow consumer.
- Fixed handling for a redirect chain longer than five hops.
- Changed handling for a redirect chain longer than five hops.
- Removed the deprecated positional argument on the export subcommand.

## 2.4.3

- Fixed handling for a redirect chain longer than five hops.
- Added the warning printed when a certificate is close to expiry.
- Fixed a crash when the queue drained while a worker was still parked.
- Added the warning printed when a certificate is close to expiry.

## 2.4.2

- Fixed the deprecated positional argument on the export subcommand.
- Removed the warning printed when a certificate is close to expiry.
- Removed a crash when the queue drained while a worker was still parked.

## 2.4.1

- Changed handling for a redirect chain longer than five hops.
- Removed a crash when the queue drained while a worker was still parked.

## 2.4.0

- Fixed the deprecated positional argument on the export subcommand.
- Documented the deprecated positional argument on the export subcommand.
- Changed a crash when the queue drained while a worker was still parked.

## 2.3.7

- Fixed the deprecated positional argument on the export subcommand.
- Documented a memory leak in the long poll listener under a slow consumer.

## 2.3.6

- Changed a memory leak in the long poll listener under a slow consumer.
- Added the flag that disabled colour output on a terminal that supports it.
- Documented a memory leak in the long poll listener under a slow consumer.

## 2.3.5

- Documented support for reading the endpoint out of the environment instead of the flag.
- Fixed the deprecated positional argument on the export subcommand.
- Fixed the warning printed when a certificate is close to expiry.
- Documented the warning printed when a certificate is close to expiry.

## 2.3.4

- Documented the flag that disabled colour output on a terminal that supports it.
- Documented a crash when the queue drained while a worker was still parked.
- Removed a crash when the queue drained while a worker was still parked.
- Fixed the deprecated positional argument on the export subcommand.

## 2.3.3

- Removed support for reading the endpoint out of the environment instead of the flag.
- Added an off by one in the pagination cursor that skipped the final page.
- Fixed handling for a redirect chain longer than five hops.
- Changed support for reading the endpoint out of the environment instead of the flag.

## 2.3.2

- Changed an off by one in the pagination cursor that skipped the final page.
- Documented handling for a redirect chain longer than five hops.
- Removed an off by one in the pagination cursor that skipped the final page.

## 2.3.1

- Documented a crash when the queue drained while a worker was still parked.
- Fixed a memory leak in the long poll listener under a slow consumer.
- Added the flag that disabled colour output on a terminal that supports it.
- Changed the flag that disabled colour output on a terminal that supports it.

## 2.3.0

- Changed the flag that disabled colour output on a terminal that supports it.
- Removed the deprecated positional argument on the export subcommand.

## 2.2.7

- Changed handling for a redirect chain longer than five hops.
- Fixed the deprecated positional argument on the export subcommand.

## 2.2.6

- Documented a crash when the queue drained while a worker was still parked.
- Changed the warning printed when a certificate is close to expiry.
- Fixed handling for a redirect chain longer than five hops.
- Added support for reading the endpoint out of the environment instead of the flag.

## 2.2.5

- Added handling for a redirect chain longer than five hops.
- Documented handling for a redirect chain longer than five hops.
- Added a crash when the queue drained while a worker was still parked.

## 2.2.4

- Changed the deprecated positional argument on the export subcommand.
- Removed support for reading the endpoint out of the environment instead of the flag.
- Changed a crash when the queue drained while a worker was still parked.

## 2.2.3

- Fixed the deprecated positional argument on the export subcommand.
- Added the flag that disabled colour output on a terminal that supports it.
- Added handling for a redirect chain longer than five hops.
- Added an off by one in the pagination cursor that skipped the final page.

## 2.2.2

- Removed an off by one in the pagination cursor that skipped the final page.
- Added the deprecated positional argument on the export subcommand.
- Changed the flag that disabled colour output on a terminal that supports it.

## 2.2.1

- Documented a crash when the queue drained while a worker was still parked.
- Fixed support for reading the endpoint out of the environment instead of the flag.

## 2.2.0

- Fixed an off by one in the pagination cursor that skipped the final page.
- Added the flag that disabled colour output on a terminal that supports it.
- Removed the flag that disabled colour output on a terminal that supports it.
- Removed a crash when the queue drained while a worker was still parked.

## 2.1.7

- Removed support for reading the endpoint out of the environment instead of the flag.
- Removed the warning printed when a certificate is close to expiry.
- Removed the deprecated positional argument on the export subcommand.
- Documented support for reading the endpoint out of the environment instead of the flag.

## 2.1.6

- Fixed support for reading the endpoint out of the environment instead of the flag.
- Documented the deprecated positional argument on the export subcommand.
- Added handling for a redirect chain longer than five hops.

## 2.1.5

- Fixed the flag that disabled colour output on a terminal that supports it.
- Added the warning printed when a certificate is close to expiry.

## 2.1.4

- Removed a memory leak in the long poll listener under a slow consumer.
- Fixed support for reading the endpoint out of the environment instead of the flag.
- Added the deprecated positional argument on the export subcommand.

## 2.1.3

- Removed the deprecated positional argument on the export subcommand.
- Added the warning printed when a certificate is close to expiry.
- Fixed handling for a redirect chain longer than five hops.
- Removed the flag that disabled colour output on a terminal that supports it.

## 2.1.2

- Removed a crash when the queue drained while a worker was still parked.
- Changed an off by one in the pagination cursor that skipped the final page.
- Changed a crash when the queue drained while a worker was still parked.

## 2.1.1

- Fixed an off by one in the pagination cursor that skipped the final page.
- Removed support for reading the endpoint out of the environment instead of the flag.
- Changed support for reading the endpoint out of the environment instead of the flag.
- Changed the flag that disabled colour output on a terminal that supports it.

## 2.1.0

- Documented the warning printed when a certificate is close to expiry.
- Changed an off by one in the pagination cursor that skipped the final page.
- Documented support for reading the endpoint out of the environment instead of the flag.

## 2.0.7

- Changed the deprecated positional argument on the export subcommand.
- Fixed a memory leak in the long poll listener under a slow consumer.
- Documented handling for a redirect chain longer than five hops.

## 2.0.6

- Fixed the flag that disabled colour output on a terminal that supports it.
- Fixed support for reading the endpoint out of the environment instead of the flag.

## 2.0.5

- Documented handling for a redirect chain longer than five hops.
- Changed handling for a redirect chain longer than five hops.
- Added a memory leak in the long poll listener under a slow consumer.
- Removed an off by one in the pagination cursor that skipped the final page.

## 2.0.4

- Removed the flag that disabled colour output on a terminal that supports it.
- Changed support for reading the endpoint out of the environment instead of the flag.
- Fixed a crash when the queue drained while a worker was still parked.
- Fixed the flag that disabled colour output on a terminal that supports it.

## 2.0.3

- Changed a memory leak in the long poll listener under a slow consumer.
- Removed the warning printed when a certificate is close to expiry.

## 2.0.2

- Documented handling for a redirect chain longer than five hops.
- Changed support for reading the endpoint out of the environment instead of the flag.

## 2.0.1

- Removed a memory leak in the long poll listener under a slow consumer.
- Removed the deprecated positional argument on the export subcommand.
- Removed the deprecated positional argument on the export subcommand.
